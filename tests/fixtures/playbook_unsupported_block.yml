# block + rescue is supported as of v0.0.10. block + always (or rescue +
# always) is still rejected; v0.0.11 is the planned home for always:.
- name: bad
  hosts: localhost
  gather_facts: no
  tasks:
    - name: try
      block:
        - name: inner
          ansible.builtin.ping:
      always:
        - name: cleanup
          ansible.builtin.ping:
